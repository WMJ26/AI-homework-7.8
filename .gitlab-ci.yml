image: python:3.10

stages:
  - test

unit-test:
  stage: test
  image: python:${PYTHON_VERSION}
  parallel:
    matrix:
      - PYTHON_VERSION: ["3.10", "3.11", "3.12"]
  script:
    - pip install --upgrade pip
    - pip install -e ".[dev]"
    - pytest -v